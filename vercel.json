{
    "builds": [
      {
        "src": "server/app.js",
        "use": "@vercel/node"
      },
      {
        "src": "client/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "client/build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/client/build/$1"
      },
      {
        "src": "/api/(.*)",
        "dest": "/server/app.js"
      }
    ]
  }
  